# Depth-Sweep Autoresearch Program

You are a Sonnet model executing an autonomous research loop designed by Opus 4.7. The plan is in this document. Follow it; do not improvise on the high-level structure. You may make tactical decisions inside each step (e.g. how to format a result row), but the variants, machine assignments, and procedure are fixed.

---

## Part 1 — Chat-state summary (for post-compaction continuity)

You may inherit this plan after the parent conversation has been compacted. Here is the context you need.

### What was done immediately before this plan

1. **`probe_musicality.py` was redesigned and committed** (commit `e5f12af`). Probe 10 (chroma R²) now uses **per-time-slice regression** instead of mean-pooling across all patches. At each Swin level L: reshape patches `(P*T, dim)` → `(P_pitch, T_time, dim)` → transpose & flatten → `(T_time, 256)` features by concatenating pitch patches per time column. Regress each slice's 256-dim feature → 12-dim chroma. Use `GroupKFold(n_splits=5)` so train/test split respects song boundaries. The output table now has a `T_slices` column. This is the version of `probe_musicality.py` that will be deployed automatically by `launch.sh probe`.

2. **Five probe runs completed and analyzed** with the new probe code:

   | Model | Run dir | Host | Notes |
   |---|---|---|---|
   | MRJ-12 | `exp26_z1olvN` | lecun | baseline, fact=0, lambd=0.2, depths=[2,2,2,6,2,1] |
   | MRJ-12∧ | `exp25_edckd7` | lecun | fact=0.5, lambd=0.2, depths=[2,2,2,6,2,1] |
   | MRJ-48 | `enc48_kDrJLt` | razer-docker | fact=0, shift=48, depths=[2,2,2,6,2,2] |
   | MRJ-48∧ | `exp26-wide3` | razer-docker | fact=0.5, shift=48, lambd=0.15, depths=[2,2,2,6,2,2] |
   | lowsigreg1 | `lowsigreg1_HmxbGJ` | tsrazer-ts-docker | fact=0.5, lambd=0.15, shift=12, depths=[2,2,2,6,2,1] |

   Probe results were generated by:
   ```
   bash scripts/launch.sh <host> probe <run_dir> probe_<tag> --no_transposition --no_time --no_emopia --no_melody --no_wandb
   ```
   Output landed at `~/runs/midi-rae/probe_<tag>_<suffix>/stormbird_results.log` on each machine.

3. **Key finding (Probe 10, L5 chroma)**:

   | Model | L5 chroma R² | depths | shift | fact |
   |---|---|---|---|---|
   | MRJ-12∧ | +0.612 | [2,2,2,6,2,**1**] | 12 | 0.5 |
   | MRJ-48∧ | +0.701 | [2,2,2,6,2,**2**] | 48 | 0.5 |
   | MRJ-12 | −3.225 | [2,2,2,6,2,1] | 12 | 0 |
   | MRJ-48 | −6.277 | [2,2,2,6,2,2] | 48 | 0 |
   | lowsigreg1 | **−15.477** | [2,2,2,6,2,**1**] | 12 | **0.5** |

   The initial hypothesis was "factorization loss fixes L5 chroma". Lowsigreg1 falsified it: it had fact=0.5 and still collapsed. The remaining differences between lowsigreg1 (bad) and MRJ-48∧ (good) are `depths` (extra L5 block) and `max_shift_x` (12 vs 48). The first follow-up was already launched:

4. **lowsigreg2 launched and may still be running** on tsrazer-ts-docker:
   - Tag: `lowsigreg2_gh1jfm`
   - Identical to lowsigreg1 EXCEPT `depths: [2,2,2,6,2,1] → [2,2,2,6,2,2]`
   - All else the same: lambd=0.15, fact=0.5, shift=12, batch_size=300, 100 epochs
   - Tests whether the L5 depth block alone (not the shift=48) fixes the L5 collapse
   - **Before launching anything from this program, check whether lowsigreg2 has finished and probe it.** Treat lowsigreg2 as variant #0 of the depth sweep.

5. **Currently committed local state** (branch `autoresearch/mar16`):
   - `e5f12af` — probe_musicality per-time-slice chroma regression
   - `48e6063` — config_swin_lowsigreg depths [2,2,2,6,2,1] → [2,2,2,6,2,2]
   - Working tree has unrelated uncommitted noise (other notebooks, untracked files). Leave those alone.

### Critical operating rules (these are persistent feedback, not optional)

- **Never** `--force` on lecun or tsrazer-ts-docker. Only on razer-docker if explicitly needed.
- **razer-docker** (Razer Blade 16", 16 GB VRAM) and **tsrazer-ts-docker** (Razer Blade 18", 24 GB VRAM) are **different physical machines** and can run in parallel safely. Do not confuse them.
- **Always** commit pending config changes before launching a training run.
- **Always edit the YAML directly**; never use Hydra CLI overrides like `++flow.x=y` when launching. The YAML in the run dir must be the authoritative record.
- **Never chain bash commands with `&&` or `;`** in a single tool call — split into separate `Bash` calls. Pre-approved exceptions are noted in `.claude/settings.json`.
- **Never use pipes** in bash commands for the same reason.
- **Never use `python -c "..."`** for reads — use the `Read` tool.
- **Read files freely; never write/edit** without explicit authorization in the current message. (You have authorization in this brief to: create new `configs/config_swin_d*.yaml` files; create/append to `depth_sweep_results.md`; commit each of these.)
- **Always use `bash scripts/status.sh <host>`** to check run logs; never `ssh <host> "tail ..."`.
- **Always ask before killing** any training run.
- **Never say "fair enough."** Scott finds it passive-aggressive.
- For all bash work, the repo root is `/workspaces/ClaudeCode-Mar12/midi-rae`. Use `git -C /workspaces/ClaudeCode-Mar12/midi-rae <cmd>` for git; `cd /workspaces/ClaudeCode-Mar12/midi-rae && bash scripts/<x>` is pre-approved for launches.

---

## Part 2 — Research program

### Goal

The depths `[2,2,2,6,2,2]` in the current MRJ-48∧ config are essentially historical — nobody designed them via principled exploration. **Vary `depths` systematically while holding all other MRJ-48∧ parameters fixed**, and identify which architecture is best across the musical probe suite.

### Reference baseline (do NOT re-run)

MRJ-48∧ = `exp26-wide3` on razer-docker. Use existing probe results as the baseline row in the comparison table:

```
lambd=0.15  lambda_fact=0.5  max_shift_x=48  depths=[2,2,2,6,2,2]
batch_size=300  epochs=100  n_skip_finest_levels=2
```

Probe 10/11 selected metrics (from earlier stormbird_results.log):

| Metric | L0 | L1 | L2 | L3 | L4 | L5 |
|---|---|---|---|---|---|---|
| Chroma R² | -0.004 | 0.028 | 0.151 | 0.382 | 0.575 | 0.701 |
| Density R² | 0.357 | 0.490 | 0.682 | 0.779 | 0.818 | 0.893 |
| Root acc | 0.086 | 0.104 | 0.138 | 0.196 | 0.249 | 0.264 |
| Key acc | 0.047 | 0.059 | 0.061 | 0.093 | 0.139 | 0.178 |
| X-song ratio | 0.631 | 0.642 | 0.680 | 0.633 | 0.699 | 0.830 |
| Chord acc | 0.532 | 0.544 | 0.553 | 0.551 | 0.541 | 0.529 |

### Variants

8 configs, varying only `depths`. Each gets its own YAML based on a copy of `configs/config_swin_lowsigreg.yaml` but with **`max_shift_x` set to 48** (to match MRJ-48∧, not lowsigreg's 12).

| # | Tag suffix | Depths | Total blocks | Hypothesis |
|---|---|---|---|---|
| 1 | `d_plus1` | `[3,3,3,6,3,3]` | 21 | Naive +1 on the 2s — does uniform extra capacity help? |
| 2 | `d_plus2` | `[4,4,4,6,4,4]` | 26 | Naive +2 — user-suggested doubling of the 2s |
| 3 | `d_uniform3` | `[3,3,3,3,3,3]` | 18 | Kill the L3 bottleneck — is the bottleneck design useful? |
| 4 | `d_uniform4` | `[4,4,4,4,4,4]` | 24 | Uniform deeper, no bottleneck, ≈same total as plus2 |
| 5 | `d_deep8` | `[2,2,2,8,2,2]` | 18 | Amplify the bottleneck only |
| 6 | `d_coarse` | `[4,4,4,6,2,2]` | 22 | More coarse capacity (L0–L2 currently underperform) |
| 7 | `d_fine` | `[2,2,2,6,4,4]` | 20 | More fine capacity (L4/L5 already do most chroma work) |
| 8 | `d_pyramid` | `[2,3,4,6,4,3]` | 22 | Taper-up to center, balanced |

Plus variant **0**: lowsigreg2 (already launched, may already be done). It's similar territory to the planned variants but `max_shift_x=12` rather than 48; include it in the results table with that caveat.

### Machine assignment

Three machines run in parallel. Each runs its assigned variants sequentially.

| Variant | Machine | Wait for |
|---|---|---|
| d_plus1 | lecun | (start immediately) |
| d_coarse | lecun | d_plus1 done |
| d_pyramid | lecun | d_coarse done |
| d_uniform3 | razer-docker | (start immediately — verify idle first) |
| d_deep8 | razer-docker | d_uniform3 done |
| d_plus2 | tsrazer-ts-docker | lowsigreg2_gh1jfm done + probed |
| d_uniform4 | tsrazer-ts-docker | d_plus2 done |
| d_fine | tsrazer-ts-docker | d_uniform4 done |

Estimated wall time ~10–12 hours (3 parallel chains).
Hardware: razer-docker = Blade 16" (16 GB VRAM); tsrazer-ts-docker = Blade 18" (24 GB VRAM).
razer-docker gets the two smallest variants (18 blocks each) as a conservative VRAM assignment.
If razer-docker OOMs on either variant, reduce batch_size to 200 and relaunch (counts as one OOM total, not two strikes).

### Configuration recipe

For each variant V with depths D:

1. **Create config file** `configs/config_swin_V.yaml` by copying `configs/config_swin_lowsigreg.yaml` verbatim, then making two edits:
   - `depths: [2, 2, 2, 6, 2, 2]` → `depths: D`
   - `max_shift_x: 12` → `max_shift_x: 48`
   - Update the comment on the `lambd:` line if it mentions exp26 baseline, or leave it.

   Do not change anything else. Lambd stays at 0.15, lambda_fact stays at 0.5, batch_size stays at 300, epochs stays at 100. The user said VRAM is not a concern, so do not preemptively shrink batch_size even for d_plus2 (26 blocks).

2. **Commit**: `git -C /workspaces/ClaudeCode-Mar12/midi-rae add configs/config_swin_V.yaml` then commit with a 1-line message like:
   ```
   config_swin_V: depths D (Nblocks total)
   ```

3. **Launch encoder**:
   ```
   cd /workspaces/ClaudeCode-Mar12/midi-rae && bash scripts/launch.sh <host> enc config_swin_V V
   ```
   Note the full run tag printed (e.g. `d_plus1_aB3xYz`).

4. **Wait**:
   ```
   bash /workspaces/ClaudeCode-Mar12/midi-rae/scripts/wait.sh <host>
   ```

5. **Read final metric**: After the wait returns, get the best metric:
   ```
   bash /workspaces/ClaudeCode-Mar12/midi-rae/scripts/status.sh <host> <full_run_tag>
   ```
   The status output includes the FINISHED line with `Best metric: 0.xxxxxx`.

6. **Launch probe**:
   ```
   cd /workspaces/ClaudeCode-Mar12/midi-rae && bash scripts/launch.sh <host> probe <full_run_tag> probe_V --no_transposition --no_time --no_emopia --no_melody --no_wandb
   ```

7. **Wait for probe**:
   ```
   bash /workspaces/ClaudeCode-Mar12/midi-rae/scripts/wait.sh <host>
   ```

8. **Read probe results**:
   ```
   bash /workspaces/ClaudeCode-Mar12/midi-rae/scripts/head-remote <host> ~/runs/midi-rae/<probe_run_tag>/stormbird_results.log 120
   ```

9. **Append a row to `depth_sweep_results.md`** in the repo root. Use the schema below. Commit it.

10. **Move on to the next variant on this machine.**

### Results table schema

Create `/workspaces/ClaudeCode-Mar12/midi-rae/depth_sweep_results.md` on the first variant completion. Format:

```
# Depth-sweep results

Baseline = MRJ-48∧ (exp26-wide3 on razer-docker), depths=[2,2,2,6,2,2].
All depth-sweep variants share lambd=0.15, lambda_fact=0.5, max_shift_x=48,
batch_size=300, 100 epochs, n_skip_finest_levels=2.

## Probe 10 — Chroma R² (per-time-slice)

| Variant | Depths | Blocks | L0 | L1 | L2 | L3 | L4 | L5 |
|---|---|---|---|---|---|---|---|---|
| baseline | [2,2,2,6,2,2] | 16 | -0.004 | 0.028 | 0.151 | 0.382 | 0.575 | 0.701 |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

## Probe 11 — Key Detection (24-class accuracy)

| Variant | Depths | L0 | L1 | L2 | L3 | L4 | L5 | chance |
... etc.

## Probe 5 — Density R²

(similar table)

## Probe 2 — Root Note (12-class accuracy)

(similar table)

## Probe 4 — Cross-Song Ratio (lower = better)

(similar table)

## Best metric (encoder val loss)

| Variant | Best metric | Run tag | Host |
| ... |

## Notes

(free-form observations as you go)
```

Append the new row in each table after every variant completes. **Commit `depth_sweep_results.md` after each update** so progress is durable. Use a 1-line commit message like `depth_sweep: + d_plus1 results`.

### Termination

Run all 8 variants (plus integrate lowsigreg2 as variant 0). Do not stop early. After all 8 are done, write a short "## Summary" section to `depth_sweep_results.md` with:
- Which variant wins on each probe (chroma L4, chroma L5, key L5, density L4, root L5)
- Whether any clear pattern emerges (e.g. "uniform > bottleneck" or "fine-heavy > coarse-heavy")
- Recommended next steps

Then stop and report back.

### When to escalate to the user (instead of just continuing)

- A training run crashes for a non-OOM reason. Log it, do NOT silently relaunch; describe and ask.
- A probe result is pathologically all-at-chance (suggests encoder didn't train). Do NOT just move on.
- More than 2 variants OOM. The user said VRAM should not be an issue, so OOMs probably mean a config bug — surface it.
- You notice anything that contradicts the assumptions in this brief.

For routine progress (variant N finished, results recorded, launching N+1), just keep going — no need to narrate.

### Existing constraints to respect during this program

- The probe_musicality.py committed at `e5f12af` is the current correct version. Don't modify it during this sweep.
- The encoder/decoder/data code that nbdev compiles is fine; don't poke at it.
- Don't touch the snapshotted configs inside `~/runs/midi-rae/<tag>/configs/` on the remote hosts — those are historical records of completed runs.
- `program.md` has the MRJ run registry; don't modify it during this sweep (this is a separate experiment thread; document it here in `depth_sweep_results.md`).

---

## Part 3 — Current state (updated 2026-05-31 ~07:30)

**Completed variants (probed, results in depth_sweep_results.md):**
- lowsigreg2 (variant 0): chroma collapse at L5 (shift=12 confirmed culprit)
- d_uniform3 [3,3,3,3,3,3]: beats baseline on all L5 metrics; chroma L5=0.736
- d_plus1 [3,3,3,6,3,3]: best cross-song L5=0.599; Probe 11 incomplete after L0
- d_deep8 [2,2,2,8,2,2]: chroma L5=0.746 (new best); cross-song weaker at mid-levels
- d_plus2 [4,4,4,6,4,4]: ⚠️ chroma L5=-125.515 collapse despite shift=48; best val loss 0.334969

**Training done, probe running:**
- d_uniform4 [4,4,4,4,4,4]: val loss 0.321271 (new best); probe_d_uniform4_CN0JJ4 on tsrazer
- d_coarse [4,4,4,6,2,2]: val loss 0.321069 (tied best); probe_d_coarse_FOV8kY on lecun

**Training running:**
- d_pyramid [2,3,4,6,4,3]: d_pyramid_6X6DEz on lecun (just launched)

**Config created, waiting to launch:**
- d_fine [2,2,2,6,4,4]: config_swin_d_fine.yaml ready; launch on tsrazer after d_uniform4 probe finishes

**razer-docker:** IDLE (d_deep8 chain complete)

**Active background waits:**
- b2yf3tod6: d_uniform4 probe on tsrazer (60s poll) → on completion: read results, launch d_fine
- br0lh1bd9: d_coarse probe on lecun (60s poll) → on completion: read results
- boot40fz0: d_pyramid training on lecun (120s poll) → on completion: get metric, launch probe

**Note on probe launch syntax** (corrected from earlier error):
```
bash scripts/launch.sh <host> probe <run_dir_name> probe_<tag> --no_transposition --no_time --no_emopia --no_melody --no_wandb
```
The CONFIG arg is the encoder run directory name (e.g. `d_deep8_ZCUKtb`), NOT the config file name.

**Note on reading probe results:**
The stormbird_results.log may not always exist (it didn't exist for d_plus1). Fall back to reading run.log using head-remote with 1500+ lines due to interleaved tqdm output from parallel probe subprocesses.

## Part 4 — Original First Action (obsolete, kept for reference)

Before launching anything: **check whether lowsigreg2_gh1jfm has finished on tsrazer-ts-docker**.

```
bash /workspaces/ClaudeCode-Mar12/midi-rae/scripts/status.sh tsrazer-ts-docker lowsigreg2_gh1jfm
```

- If still running, start with lecun's chain (d_plus1) and check tsrazer periodically.
- If FINISHED, run the probe on it now (`bash scripts/launch.sh tsrazer-ts-docker probe lowsigreg2_gh1jfm probe_lowsigreg2 --no_transposition --no_time --no_emopia --no_melody --no_wandb`), then once probe is done, record results as variant #0 and start d_plus2 on tsrazer.

After that, follow the sequential procedure above for each variant on each machine.
