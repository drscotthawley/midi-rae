defaults:
  - config_swin_razer
  - _self_

preencoded: true

training:
  dec_batch_size: 200      # razer GPU; conservative start, can increase
  dec_epochs: 100
  dec_lr: 2e-3
  use_pitch_mean: true
  dec_init_ckpt: null      # fresh start; no prior dec checkpoint on razer

tag: ???
