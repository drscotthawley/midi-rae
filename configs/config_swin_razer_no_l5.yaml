defaults:
  - config_swin_razer
  - _self_

training:
  zero_levels: [5]     # always zero L5 embeddings — ablation: can decoder reconstruct without finest level?
  emb_mask_prob: 0.0   # no random masking, just the fixed L5 ablation
