# Train Decoder


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

------------------------------------------------------------------------

### PreEncodedDataset

``` python

def PreEncodedDataset(
    encoded_dir
):

```

*Load pre-encoded embeddings + images from .pt files*

------------------------------------------------------------------------

### setup_dataloaders

``` python

def setup_dataloaders(
    cfg, preencoded:bool=False
):

```

------------------------------------------------------------------------

### setup_models

``` python

def setup_models(
    cfg, device, preencoded
):

```

------------------------------------------------------------------------

### setup_tstate

``` python

def setup_tstate(
    cfg, device, decoder, discriminator, encoder:NoneType=None
):

```

*Training_state: Losses, Optimizers, Schedulers, AMP Scalers*

------------------------------------------------------------------------

### get_embeddings_batch

``` python

def get_embeddings_batch(
    batch, encoder:NoneType=None, preencoded:bool=False, device:str='cuda'
):

```

*Get embeddings + images from batch, either pre-encoded or computed
on-the-fly*

------------------------------------------------------------------------

### train_step

``` python

def train_step(
    epoch, z, img_real, decoder, discriminator, tstate, # named tuple containing optimizers, loss fns, scalers
    cfg, # config
):

```

*training step for decoder (and discriminator)*

------------------------------------------------------------------------

### train

``` python

def train(
    cfg:DictConfig
):

```
