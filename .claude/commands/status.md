Check the status of the most recent (or a specific) training run on a remote host.

Run `bash scripts/status.sh $ARGUMENTS` from `/workspaces/ClaudeCode-Mar12/midi-rae` and report the results clearly, including whether the run is still going, crashed, or finished, and the last few log lines.

Reminder: use `razer-docker` for razer (home/local network) or `razer-ts-docker` (away/Tailscale). Use `lecun` for lecun.
